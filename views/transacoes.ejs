<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/transacoes.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body>
    <!-- Barra de Navegação -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/principal" class="nav-brand">FinancerPro</a>
            
            <button class="nav-toggle" id="navToggle">
                <span class="nav-icon">☰</span>
            </button>

            <ul class="nav-menu" id="navMenu">
                <li class="nav-item">
                    <a href="/principal" class="nav-link <%= currentPage === '/principal' ? 'active' : '' %>">
                        <span class="nav-icon">📊</span>
                        Dashboard
                        <span class="nav-indicator"></span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/transacoes" class="nav-link <%= currentPage === '/transacoes' ? 'active' : '' %>">
                        <span class="nav-icon">💸</span>
                        Adicionar Transação
                        <span class="nav-indicator"></span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/historico" class="nav-link <%= currentPage === '/historico' ? 'active' : '' %>">
                        <span class="nav-icon">📈</span>
                        Histórico
                        <span class="nav-indicator"></span>
                    </a>
                </li>
            </ul>

            <div class="user-menu">
                <div class="user-avatar">JS</div>
            </div>
        </div>
    </nav>

    <div class="container">
        <!-- Header da Página -->
        <div class="page-header">
            <a href="/principal" class="back-button">
                <i class="fas fa-arrow-left"></i>
                <span>Voltar ao Dashboard</span>
            </a>
            <h1><%= titulo %></h1>
            <p class="page-subtitle">Adicione uma nova transação ao seu controle financeiro</p>
        </div>

        <!-- Card do Formulário -->
        <div class="form-card">
            <div class="card-header">
                <div class="header-icon">
                    <i class="fas fa-plus-circle"></i>
                </div>
                <h2>Nova Transação</h2>
                <p>Preencha os dados da transação</p>
            </div>

            <form class="transaction-form" action="/transacoes/adicionar" method="POST">
                <div class="form-grid">
                    <!-- Data -->
                    <div class="form-group">
                        <label for="data" class="form-label">
                            <i class="fas fa-calendar"></i>
                            Data da Transação
                        </label>
                        <div class="input-container">
                            <input type="date" id="data" name="data" class="form-input" required 
                                   value="<%= new Date().toISOString().split('T')[0] %>">
                            <div class="input-icon">
                                <i class="fas fa-calendar-day"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Descrição -->
                    <div class="form-group">
                        <label for="descricao" class="form-label">
                            <i class="fas fa-file-alt"></i>
                            Descrição
                        </label>
                        <div class="input-container">
                            <input type="text" id="descricao" name="descricao" class="form-input" 
                                   placeholder="Ex: Salário, Aluguel, Supermercado..." required>
                            <div class="input-icon">
                                <i class="fas fa-pen"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Categoria -->
                    <div class="form-group">
                        <label for="categoria" class="form-label">
                            <i class="fas fa-tag"></i>
                            Categoria
                        </label>
                        <div class="input-container">
                            <select id="categoria" name="categoria" class="form-input" required>
                                <option value="">Selecione uma categoria</option>
                                <option value="Receita">💰 Receita</option>
                                <option value="Moradia">🏠 Moradia</option>
                                <option value="Alimentação">🍽️ Alimentação</option>
                                <option value="Transporte">🚗 Transporte</option>
                                <option value="Saúde">🏥 Saúde</option>
                                <option value="Educação">📚 Educação</option>
                                <option value="Lazer">🎮 Lazer</option>
                                <option value="Investimentos">📈 Investimentos</option>
                                <option value="Outros">📦 Outros</option>
                            </select>
                            <div class="input-icon">
                                <i class="fas fa-tags"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Tipo -->
                    <div class="form-group">
                        <label for="tipo" class="form-label">
                            <i class="fas fa-exchange-alt"></i>
                            Tipo de Transação
                        </label>
                        <div class="type-selector">
                            <input type="radio" id="entrada" name="tipo" value="Entrada" class="type-radio" required>
                            <label for="entrada" class="type-label type-entrada">
                                <i class="fas fa-arrow-down"></i>
                                <span>Entrada</span>
                                <small>Dinheiro entrando</small>
                            </label>

                            <input type="radio" id="saida" name="tipo" value="Saída" class="type-radio">
                            <label for="saida" class="type-label type-saida">
                                <i class="fas fa-arrow-up"></i>
                                <span>Saída</span>
                                <small>Dinheiro saindo</small>
                            </label>
                        </div>
                    </div>

                    <!-- Valor -->
                    <div class="form-group">
                        <label for="valor" class="form-label">
                            <i class="fas fa-money-bill-wave"></i>
                            Valor
                        </label>
                        <div class="input-container">
                            <input type="number" id="valor" name="valor" class="form-input" 
                                   step="0.01" min="0.01" placeholder="0.00" required>
                            <div class="input-icon">
                                <i class="fas fa-dollar-sign"></i>
                            </div>
                            <span class="currency-symbol">R$</span>
                        </div>
                    </div>

                    <!-- Observações -->
                    <div class="form-group full-width">
                        <label for="observacoes" class="form-label">
                            <i class="fas fa-sticky-note"></i>
                            Observações (Opcional)
                        </label>
                        <div class="input-container">
                            <textarea id="observacoes" name="observacoes" class="form-textarea" 
                                      placeholder="Alguma observação adicional sobre esta transação..." 
                                      rows="3"></textarea>
                            <div class="input-icon">
                                <i class="fas fa-comment"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Ações do Formulário -->
                <div class="form-actions">
                    <button type="button" class="btn-secondary" id="btnLimpar">
                        <i class="fas fa-eraser"></i>
                        Limpar Campos
                    </button>
                    <button type="submit" class="btn-primary">
                        <i class="fas fa-check-circle"></i>
                        Adicionar Transação
                    </button>
                </div>
            </form>
        </div>

        <!-- Preview em Tempo Real -->
        <div class="preview-card">
            <div class="preview-header">
                <h3><i class="fas fa-eye"></i> Pré-visualização</h3>
                <p>Visualize como sua transação ficará</p>
            </div>
            <div class="preview-content">
                <div class="preview-item">
                    <span class="preview-label">Data:</span>
                    <span class="preview-value" id="previewData">--/--/----</span>
                </div>
                <div class="preview-item">
                    <span class="preview-label">Descrição:</span>
                    <span class="preview-value" id="previewDescricao">---</span>
                </div>
                <div class="preview-item">
                    <span class="preview-label">Categoria:</span>
                    <span class="preview-value" id="previewCategoria">---</span>
                </div>
                <div class="preview-item">
                    <span class="preview-label">Tipo:</span>
                    <span class="preview-value" id="previewTipo">---</span>
                </div>
                <div class="preview-item">
                    <span class="preview-label">Valor:</span>
                    <span class="preview-value" id="previewValor">R$ 0,00</span>
                </div>
            </div>
        </div>

        <!-- Transações Recentes -->
        <div class="recent-transactions">
            <div class="section-header">
                <h3><i class="fas fa-clock"></i> Transações Recentes</h3>
                <a href="/historico" class="view-all-link">
                    Ver todas
                    <i class="fas fa-arrow-right"></i>
                </a>
            </div>
            
            <% if (transacoes && transacoes.length > 0) { %>
                <div class="transactions-list">
                    <% transacoes.slice(-3).reverse().forEach(function(transacao) { %>
                        <div class="transaction-item">
                            <div class="transaction-icon">
                                <i class="fas <%= transacao.tipo === 'Entrada' ? 'fa-arrow-down text-success' : 'fa-arrow-up text-danger' %>"></i>
                            </div>
                            <div class="transaction-info">
                                <div class="transaction-desc"><%= transacao.descricao %></div>
                                <div class="transaction-meta">
                                    <span class="transaction-category"><%= transacao.categoria %></span>
                                    <span class="transaction-date"><%= transacao.data %></span>
                                </div>
                            </div>
                            <div class="transaction-amount <%= transacao.tipo === 'Entrada' ? 'text-success' : 'text-danger' %>">
                                R$ <%= transacao.valor.toFixed(2) %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon">
                        <i class="fas fa-receipt"></i>
                    </div>
                    <h4>Nenhuma transação recente</h4>
                    <p>As transações que você adicionar aparecerão aqui</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Notificação -->
    <div class="notification" id="notification">
        <div class="notification-content">
            <i class="fas fa-check-circle"></i>
            <span class="notification-message">Transação adicionada com sucesso!</span>
        </div>
    </div>

    <script src="/js/transacoes.js"></script>
</body>
</html> 